import SectionTitle from "../../../components/SectionTitle/SectionTitle";

const Users = () => {
    return (
        <div>
            <SectionTitle heading="Manage Users" subheading="Manage all the users, see their subscription, make admin and details"></SectionTitle>
        </div>
    );
};

export default Users;